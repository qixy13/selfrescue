#### 面经总结

11.24旷视 后端研发

主要技术栈：go/c++

问题：mysql调优有没有处理过

​			处理过最大的数据量是多少

​			有没有了解过缓存机制是怎么产生的

算法：二分查找，冒泡排序，快速排序

主要问题：技术栈不匹配



##### String StringBuilder StringBuffer的区别

String创建时会在字符串池中寻找有没有对象，没有的话会新建一个，并且在栈中新建一个引用变量，将这个变量指向字符串池中的元素

String s = new String("ABC");与String s = "ABC";不一样，前者创建两个对象，后者只创建一个

StringBuilder为了避免String在改动时会创建许多次String对象导致内存浪费甚至溢出

StringBuffer是synchronized StringBuilder，可以多线程访问

```java
String s = "";
for(int i=0;i<10000;i++){
    s+="hello";
}
//上面这段代码会创建10000个新的String对象，导致内存极大的浪费
//使用StringBuilder则不会创建多个String对象，只会创建一个StringBuilder对象
StringBuilder sb = new StringBuilder("");
for(i=0;i<10000;i++){
    s+="hello";
}

```

##### 序列化的过程及作用

作用：

1. 能够将对象输出且保存在硬盘上，以文件的形式永久保存
2. 能够在网络上传递对象数据

反序列化时如果添加了新属性，则必须使用serialVersionUID，重新进行序列化，才能反序列化

transient关键字：写在变量声明前面，用于阻止变量被序列化

##### cookie与session

cookie：一般用来保存用户信息，例如用户登陆的状态信息，Token

session：通过服务器记录用户的状态，安全性更高

#####  hashtable hashmap concurrenthashmap

hashtable：在修改数据时加了synchronized锁，key-value都不能为null，线程安全

hashmap：底层数组+链表实现，可以为null，但线程不安全，大小一定为2^n

原因：计算数组下标方法为（n-1）&hash，而这种运算的前提是n（数组长度）为2的次幂

concurrenthashmap：key不能为null，底层采用分段数组+链表实现，线程安全。分成的多个segment分别是一个hashentry，value变量是volatile的，效率高于hashtable（采用了锁分离技术），同一时刻可以由多个线程对其他未获得锁的部分进行操作

##### 类加载机制

加载：通过一个类的全限定名来获取此类的二进制字节流；将这个字节流所代表的静态存储结构转化为方法区的运行时存储结构；在内存中生成一个代表这个类的Class对象，作为方法区这个类的各种数据的访问入口

验证：确保class文件的字节流中包含的信息符合当前虚拟机的要求，并不会危害虚拟机自身的安全

准备：为类变量分配内存并设置初始值，使用的是方法区的内存

解析：将常量池的符号引用替换为直接引用的过程

初始化：虚拟机执行类构造器<clinit>()方法的过程

类加载器：启动类加载器：被java程序直接引用，把加载请求委派给启动类加载器；扩展类加载器；应用程序类加载器

###### 双亲委派模型

应用程序都是由三种类加载器相互配合进行的，但是也可以加入自己定义的类加载器

下图是双亲加载模型的示例

![https://pic2.zhimg.com/80/v2-82aa496f4ea4791961daddfa7765a9f5_hd.jpg](https://pic2.zhimg.com/80/v2-82aa496f4ea4791961daddfa7765a9f5_hd.jpg)

该模型要求除了顶层的启动类加载器外，其余的类加载器都应有自己的父类加载器。这里类加载器之间的父子关系一般通过组合（Composition）关系来实现，而不是通过继承（Inheritance）的关系实现。

一个类加载器首先将类加载请求传送到父类加载器，只有当父类加载器无法完成类加载请求时才尝试加载。

自定义类加载器实现

FileSystemClassLoader用于加载文件系统上的类，先根据全限定名在文件系统上查找类的字节代码文件，通过读取文件内容，使用defineclass()方法将字节代码转换成java.lang.Class的实例

##### TCP拥塞控制

了解当前的网络带宽，TCP发送方发送拥塞窗口，动态变化

慢开始-拥塞避免（cwnd每次+1）-快重传及快恢复

##### 网页加载

DNS查找解析-与服务器TCP-http请求-服务器返回http报文-浏览器解析渲染页面-连接结束

##### synchronized与volatile

1. volatile本质是在告诉jvm当前变量在寄存器中的值是不确定的，需要从主存中读取，synchronized则是锁定当前变量，只有当前线程可以访问该变量,其他线程被阻塞住。
2. volatile仅能使用在变量级别，synchronized则可以使用在变量，方法。
3. volatile仅能实现变量的修改可见性，而synchronized则可以保证变量的修改可见性和原子性。《Java编程思想》上说，定义long或double变量时，如果使用volatile关键字，就会获得（简单的赋值与返回操作）原子性。
4. volatile不会造成线程的阻塞，而synchronized可能会造成线程的阻塞。
5. 当一个域的值依赖于它之前的值时，volatile就无法工作了，如n=n+1,n++等。如果某个域的值受到其他域的值的限制，那么volatile也无法工作，如Range类的lower和upper边界，必须遵循lower<=upper的限制。
6. 用volatile而不是synchronized的唯一安全的情况是类中只有一个可变的域。
7. volatile通过添加内存屏障的方式禁止指令的重排序。

数据库幻读与不可重复读

幻读：另一个并发事务在新增或删除某些记录，当前事务进程随后就会发现一些本不存在的记录，好像幻觉一样